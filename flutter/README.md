# DermaScann AI (Flutter)

This folder contains a Flutter rewrite of the existing Expo React Native app in the repo.

## Run

```bash
cd flutter
flutter pub get
flutter run
```

## Build APK (Android)

```bash
flutter build apk --debug
```

APK output:
`build/app/outputs/flutter-apk/app-debug.apk`

